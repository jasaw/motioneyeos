diff -rupEbwBN -x.git host-e2fsprogs-1.43.7.org/misc/create_inode.c host-e2fsprogs-1.43.7/misc/create_inode.c
--- host-e2fsprogs-1.43.7.org/misc/create_inode.c	2017-10-16 16:45:45.000000000 +1100
+++ host-e2fsprogs-1.43.7/misc/create_inode.c	2019-10-18 14:40:10.832272147 +1100
@@ -396,7 +396,7 @@ static ssize_t my_pread(int fd, void *bu
 }
 #endif /* !defined HAVE_PREAD64 && !defined HAVE_PREAD */
 
-static errcode_t copy_file_range(ext2_filsys fs, int fd, ext2_file_t e2_file,
+static errcode_t copy_file_chunk(ext2_filsys fs, int fd, ext2_file_t e2_file,
 				 off_t start, off_t end, char *buf,
 				 char *zerobuf)
 {
@@ -470,7 +470,7 @@ static errcode_t try_lseek_copy(ext2_fil
 
 		data_blk = data & ~(fs->blocksize - 1);
 		hole_blk = (hole + (fs->blocksize - 1)) & ~(fs->blocksize - 1);
-		err = copy_file_range(fs, fd, e2_file, data_blk, hole_blk, buf,
+		err = copy_file_chunk(fs, fd, e2_file, data_blk, hole_blk, buf,
 				      zerobuf);
 		if (err)
 			return err;
@@ -521,7 +521,7 @@ static errcode_t try_fiemap_copy(ext2_fi
 			goto out;
 		for (i = 0, ext = ext_buf; i < fiemap_buf->fm_mapped_extents;
 		     i++, ext++) {
-			err = copy_file_range(fs, fd, e2_file, ext->fe_logical,
+			err = copy_file_chunk(fs, fd, e2_file, ext->fe_logical,
 					      ext->fe_logical + ext->fe_length,
 					      buf, zerobuf);
 			if (err)
@@ -574,7 +574,7 @@ static errcode_t copy_file(ext2_filsys f
 		goto out;
 #endif
 
-	err = copy_file_range(fs, fd, e2_file, 0, statbuf->st_size, buf,
+	err = copy_file_chunk(fs, fd, e2_file, 0, statbuf->st_size, buf,
 			      zerobuf);
 out:
 	ext2fs_free_mem(&zerobuf);
